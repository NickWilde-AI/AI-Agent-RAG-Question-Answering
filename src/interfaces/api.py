"""
interfaces/api.py — 兼容层：把 `src.api` 再导出一次（旧入口 / 文档链接用）

================================================================================
【在「简历第一条」里的位置】
================================================================================
无独立逻辑；`from ..api import *` 与 Java `export` / Kotlin `typealias` 到单一真源类似。

================================================================================
【从 Java/Kotlin 读 Python】
================================================================================
- `from ..api import *`：通配导入，会带入 `api` 模块里 `_` 不开头的全部 public 名；`# noqa: F401,F403` 告诉 linter 忽略未使用/通配警告。

API 模块映射（兼容入口）。
"""

from ..api import *  # noqa: F401,F403

